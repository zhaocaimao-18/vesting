const DECIMAL = 10;
const TOKEN_NAME = "Token1";
const SYMBOL_TOKEN = "T1";

const DEVELOPMENT_ENVIRONMENT_CHAINS = ["hardhat", "local"]

module.exports = {
    DECIMAL,
    TOKEN_NAME,
    SYMBOL_TOKEN,
    DEVELOPMENT_ENVIRONMENT_CHAINS
}